Name: Rihan Chen
Student ID: 5078956

Instruction:
mysmosvm:
python3 /path/to/your/data/set num_of_run
details:
Run the codes above in shell or terminal. The runtime and corresponding standard deviation will be printed in the terminal.
The file that contains values for objective function will be written into the dir which is the same as mysmosvm.py.

mysgdsvm:
python3 /path/to/your/data/set size_of_minibatch num_of_run
details:
Run the codes above in shell or terminal. Different argv are splited by space. The runtime and corresponding standard deviation will be printed in the terminal.
The file that contains values for objective function will be written into the dir which is the same as mysgdsvm.py.
